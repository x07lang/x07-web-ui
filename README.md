# x07-web-ui

This repo tracks Phase 0 contracts for running an x07 UI reducer as a pure state machine.

The spec-only WIT package lives in `wit/`:

- `x07:web-ui@0.1.0`: JSON-bytes boundary (`UTF-8`) with `init` and `event`.
